name: Compile code
on:
  release:
    types:
      - created

jobs:
  build:
    name: Compile Code
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Compile code into MSI package
        run: python compile.py bdist_msi

      - name: Move Files
        run: |
          $env:FILE_NAME="TroveFileExtractor"
          $env:RELEASE_NAME="${{ github.event.release.name }}"
          $env:NEW_FILE_NAME="${env:FILE_NAME}-${env:RELEASE_NAME}-win64.msi"
          ls
          ls dist
          Move-Item "dist/${ env:NEW_FILE_NAME }" ".\${ env:NEW_FILE_NAME }"
          ls
          ls dist

      - name: Upload file to release
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: "TroveFileExtractor-${{ github.event.release.name }}-win64.msi"
          asset_name: "TroveFileExtractor-${{ github.event.release.name }}-win64.msi"
          asset_content_type: application/x-msi
        env:
          GITHUB_TOKEN: ${{ secrets.COMPILER }}